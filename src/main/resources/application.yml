spring:
  datasource:
    url: jdbc:postgresql://localhost:5444/cypressworkshop
    username: cypress
    password: cypress
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
